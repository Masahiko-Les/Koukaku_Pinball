import Foundation

/// Global switches for the game. Flip `debugMode` on during development to see
/// live smile values, physics wireframes, and FPS overlaid on the table.
enum GameConfig {
    static let debugMode = false
}
